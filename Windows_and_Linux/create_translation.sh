#!/bin/bash

# English-only version - no compilation needed
# The application will use English strings directly
echo "English-only version: No translation compilation needed."
echo "Application will use English strings directly."
